Context for dev (Astro):

- Framework: Astro (use existing project structure & components).  
- Do NOT change global styles or add libs. Reuse Tools page styles/components.

- Move current homepage -> /tools (preserve markup + styles).

- Create new homepage (/) with locked sections:
  1) Hero (2-col)
     - Left: Headline, Subheadline, CTA "Mulai dengan Remote Kits"
     - Right: Promo banner image (use file: /mnt/data/Screenshot 2025-11-22 at 23.35.35.png)
     - No search, no slider
  2) Featured Remote Kits
     - 4 small product cards (4-grid) -> CTA to /remote-kits
  3) Guides (dominant)
     - Category chips, 1 featured guide, 4–6 guide cards -> CTA to /guides
     - Guides = SEO articles (informational, soft CTA to /remote-kits)
  4) Tools preview
     - 6 compact tool cards pulled from existing data -> CTA to /tools
  5) Footer
     - Reuse existing footer; include "Submit as Author" CTA linking to /about

- Routes to ensure:
  /, /tools, /guides, /guides/[slug], /remote-kits, /about

- Data: pull tools data from current tools page; hardcode initial guides/cards if necessary.

Deliverable: PR that moves old homepage to /tools and adds new /index.astro matching above, using only existing components/styles.
